# Machine Learning, Deep Learning (U-Net) & Medical Image Processing
# Khuyến nghị: Python 3.10

-r requirements.txt

# Medical Imaging (Image I/O)
SimpleITK>=2.3.0

# Computer Vision & Deep Learning (U-Net)
opencv-python>=4.8.0
tensorflow>=2.8.0,<2.16.0

# Interactive Notebooks
ipykernel>=6.25.0
jupyter>=1.0.0

# -----------------------------------------------------------------------------
# LƯU Ý ĐẶC BIỆT VỀ PYRADIOMICS:
# Thư viện 'pyradiomics' trên PyPI gặp lỗi build isolation (ModuleNotFoundError: No module named 'numpy')
# và yêu cầu C++ Build Tools khi build từ source trên Windows.
#
# * ĐỂ CHẠY DASHBOARD: KHÔNG CẦN pyradiomics (Dữ liệu đã trích xuất sẵn trong .db và .csv).
# * NẾU BẠN CẦN TRÍCH XUẤT ẢNH MỚI (chạy notebook trong ML/Feature Extraction/):
#   - Cách 1 (Khuyên dùng nhất trên Windows): 
#       conda install -c conda-forge pyradiomics
#   - Cách 2 (Nếu dùng pip):
#       pip install wheel numpy
#       pip install pyradiomics --no-build-isolation
# -----------------------------------------------------------------------------
# pyradiomics>=3.0.1
